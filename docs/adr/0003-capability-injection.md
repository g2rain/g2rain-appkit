# ADR-0003：通过能力注入隔离业务应用

- 状态：已接受
- 日期：2026-09-09

## 背景

公共候选代码会直接依赖应用 Store、国际化实例、Token、路由和领域 API。原样迁移只会把应用耦合转移到 npm 包。

## 决策

公共包通过 Props、事件、Slots、Provider 和工厂参数获取外部能力，不导入具体应用实现或目录别名。

## 结果

- 公共包可以在不同应用和 Playground 中独立测试。
- 应用保留认证、导航、领域 API 和状态管理的控制权。
- 装配代码略有增加，但依赖关系可见且可替换。

